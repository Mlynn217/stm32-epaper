# Prerequisites
*.d

# Object files
*.o
*.ko
*.obj
*.elf

# Linker output
*.ilk
*.map
*.exp

# Precompiled Headers
*.gch
*.pch

# Libraries
*.lib
*.a
*.la
*.lo

# Shared objects (inc. Windows DLLs)
*.dll
*.so
*.so.*
*.dylib

# Executables
*.exe
*.out
*.app
*.i*86
*.x86_64
*.hex

# Debug files
*.dSYM/
*.su
*.idb
*.pdb

# Kernel Module Compile Results
*.mod*
*.cmd
.tmp_versions/
modules.order
Module.symvers
Mkfile.old
dkms.conf

# debug information files
*.dwo

# CMake build output (see CMakePresets.json: binaryDir is build/<preset>)
/build/
/compile_commands.json

# minicom session capture (see .vscode/tasks.json, launch.json)
/uart.log
minicom-*.log

# IAR EWARM build output, if the EWARM/ project is ever opened in IAR
# (not part of our normal workflow - we build via CMake, see README)
EWARM/*/Exe/
EWARM/*/Obj/
EWARM/*/List/
EWARM/settings/
EWARM/*.dep

# OS / editor cruft
.DS_Store
*~
*.swp

# KiCad per-user/local state (project files themselves are tracked, see hardware/kicad/)
*.kicad_prl
*-backups/
fp-info-cache
_autosave-*
~*.lck
__pycache__/

# Enclosure build outputs (regenerate with hardware/enclosure/build.py)
hardware/enclosure/out/

# Last routing attempt DRC report (hardware/scripts/route_loop.py)
hardware/kicad/drc-last.json
